# HealthMate 答辩高频问题精简版

> 适合汇报前快速背诵。重点覆盖最可能被问到的功能设计、AI 能力、知识增强、风险边界和业务闭环；传统前后端实现细节只保留必要内容。

## 一、项目定位与需求

### 1. 这个项目主要解决什么问题？

解决用户日常健康管理中记录麻烦、数据分散、建议泛化、难以坚持行动的问题。系统通过自然语言记录、AI 解析、趋势分析和任务打卡形成健康管理闭环。

### 2. 项目的目标用户是谁？

主要面向亚健康人群、健身初学者和职场人群，帮助他们管理睡眠、饮食、运动等日常健康行为。

### 3. 这个系统是不是医疗诊断系统？

不是。系统定位是日常健康辅助管理，不提供疾病诊断、处方、用药调整或治疗方案。

### 4. 为什么要明确非医疗定位？

因为用户可能输入病痛或症状信息，系统不能代替医生。我们通过高危内容识别拦截这类输入，并提示用户及时就医。

### 5. 项目的核心业务闭环是什么？

健康档案 -> 自然语言记录 -> AI 解析预览 -> 用户确认保存 -> 趋势分析 -> AI 建议 -> 候选任务 -> 用户选择 -> 打卡反馈。

### 6. 项目相比普通健康记录工具有什么特点？

普通工具偏手动表单和数据展示，本项目重点是 AI 结构化解析、知识增强建议、任务候选生成和用户确认机制。

### 7. 为什么要做任务打卡？

健康建议如果不能转化为行动，价值有限。任务打卡把建议拆成当天可执行的小目标，帮助用户形成反馈闭环。

## 二、核心功能设计

### 8. 用户首次使用流程是什么？

注册或登录后完善健康档案，然后输入健康记录，系统解析并保存，之后可以查看首页、趋势、AI 建议和任务。

### 9. 健康档案包含哪些信息？

包含性别、身高、体重、健康目标、伤病史和过敏史等。

### 10. 为什么伤病史和过敏史要拆成两个字段？

因为它们影响不同类型建议：伤病史主要影响运动建议，过敏史主要影响饮食建议，拆分后更利于个性化生成。

### 11. 健康记录支持哪些内容？

支持睡眠、饮食、运动以及混合记录，保存字段包括原始输入、记录时间、摄入热量、消耗热量、睡眠分钟、详情 JSON、标签和解析置信度。

### 12. 用户一天能记录多次吗？

可以。系统支持同一天多条健康记录，符合早中晚分别记录饮食、运动和睡眠的真实场景。

### 13. 为什么记录不能按天覆盖？

如果按天覆盖，会丢失用户当天多次输入的信息，影响趋势分析和 AI 建议准确性。

### 14. 用户可以删除错误记录吗？

后端支持健康记录历史查询和删除接口，用户可以删除误输入或过时记录，避免影响后续建议。

### 15. 趋势分析如何处理多条记录？

后端按日期聚合记录。摄入和消耗可以按天求和，标签合并统计，睡眠根据记录进行日维度汇总。

### 16. 首页主要展示什么？

展示今日任务、完成进度、最近健康记录、最新建议和趋势摘要。

### 17. 趋势页主要展示什么？

展示睡眠趋势、热量摄入/消耗趋势、健康标签分布，并支持周/月维度切换和 CSV 导出。

### 18. 为什么建议和任务要分开？

建议是解释和分析，任务是行动项。分开后用户可以先理解建议，再决定是否生成和接受任务，更可控。

### 19. AI 任务会自动写入任务列表吗？

不会。系统先生成候选任务，用户勾选接受后才写入。

### 20. 用户接受新任务后怎么处理旧任务？

后端会用用户选择的新任务覆盖或归档当前未完成任务，避免任务越积越多。

## 三、AI 健康记录解析

### 21. AI 解析的目标是什么？

把用户自然语言输入转换成适合落库的结构化健康记录字段。

### 22. 为什么不用纯表单录入？

表单准确但门槛高，自然语言更符合用户日常表达，AI 可以降低输入成本。

### 23. AI 解析会直接保存到数据库吗？

不会。解析接口只返回预览结果，用户确认或修改后才保存。

### 24. 为什么需要用户确认？

AI 可能理解错误，确认流程可以防止错误数据直接入库，提高数据可靠性。

### 25. Prompt 里为什么要包含数据库表结构？

为了让模型按实际落库字段输出，减少无关内容，提高结构化结果和后端模型的匹配度。

### 26. 解析结果包含哪些关键信息？

包括是否可保存、置信度、失败原因、优化建议、解析警告和 previewData 结构化预览数据。

### 27. 如果 AI 解析不出有效信息怎么办？

返回 `shouldSave=false`，同时给出失败原因和优化输入建议，前端提示用户补充更明确的内容，不保存该记录。

### 28. 什么情况下解析置信度会低？

输入缺少明确时间、数量、类型或健康事件，例如“今天还行”这种无法落库的信息。

### 29. AI 返回格式不稳定怎么办？

后端会做字段归一化和校验，只接收允许的字段，避免直接信任模型输出。

### 30. 如果 LLM 不可用怎么办？

系统有规则解析兜底，保证基础流程仍可运行。

## 四、高危内容与安全边界

### 31. 什么是高危内容？

指超出日常健康管理范围的输入，尤其是病痛、明显症状、疾病、用药、心理危机或急性风险相关内容。

### 32. 高危内容如何识别？

优先用大模型判断，正则关键词作为兜底，提升识别稳定性。

### 33. 用户输入“胸口疼、呼吸困难”会怎样？

系统会识别为高危内容，拒绝作为普通健康记录保存，并提示及时就医。

### 34. 为什么高危内容不保存？

这类内容不适合作为普通健康打卡处理，系统不应误导用户依赖 AI 建议，而应提示就医。

### 35. 前端也做风险词检测吗？

前端有本地预检查，用于更早提示用户；但最终以后端判断为准。

### 36. 为什么前后端都要做风险识别？

前端提升交互体验，后端保证安全边界，防止绕过前端直接调用接口。

## 五、知识增强 RAG

### 37. 项目里的 RAG 是什么？

是轻量级知识增强机制，从本地健康知识库检索相关片段，并注入 AI Prompt，辅助解析和建议生成。

### 38. 为什么要引入 RAG？

为了让 AI 不只依赖模型自身知识，而是结合项目维护的健康知识库，提高建议依据性、场景贴合度和边界控制能力。

### 39. RAG 用在哪些地方？

主要用于健康记录 AI 解析和 AI 健康建议生成。

### 40. RAG 是完整的健康问答系统吗？

不是。它是现有 AI 流程的知识增强层，不是独立多轮问答系统。

### 41. 知识库在哪里？

在 `backend/app/data/knowledge/`，以 Markdown 文件维护。

### 42. 知识库包含哪些主题？

包括睡眠、营养、运动、体重管理、健康风险、慢病生活方式边界、心理压力、健康记录规范和来源说明。

### 43. 为什么用 Markdown 做知识库？

Markdown 轻量、易维护、方便按标题切分知识块，适合课程项目和小规模知识库。

### 44. KnowledgeService 做什么？

负责加载知识库、切分知识块、执行检索，并把结果渲染成 Prompt 可用的知识上下文。

### 45. RAG 检索用什么方法？

优先使用 `sentence-transformers` 做 embedding 语义检索；如果依赖或模型不可用，会降级为关键词检索。

### 46. 为什么保留关键词检索兜底？

为了保证缺少 embedding 环境时系统仍能运行，不影响核心业务链路。

### 47. RAG 会直接决定 AI 输出吗？

不会。RAG 只是提供参考知识上下文，最终仍由 LLM、后端校验、风险识别和用户确认共同控制。

### 48. RAG 和 fine-tuning 有什么区别？

RAG 是检索外部知识并注入 Prompt，不改变模型参数；fine-tuning 是训练模型参数。RAG 更轻量、可控、便于更新知识。

### 49. 为什么不用 LangChain？

当前只需要本地知识加载、切分、检索和 Prompt 注入，自研 KnowledgeService 更轻量，也更容易展示核心实现逻辑。

### 50. RAG 的核心代码在哪里？

核心在 `backend/app/services/knowledge.py`，接入点在 `advice.py` 和 `health_parse_ai.py`。

## 六、AI 建议与任务生成

### 51. AI 建议基于哪些信息？

基于用户档案、健康记录、任务完成情况、历史建议、健康总结和知识库检索上下文。

### 52. AI 建议为什么用 SSE？

SSE 适合服务端向前端单向推送生成文本，能让建议逐步展示，减少等待感。

### 53. 任务生成基于哪些信息？

基于用户档案、所有健康记录、所有任务及状态、今日已完成任务、今日未完成任务、最新建议和总结。

### 54. 如何避免生成重复任务？

后端会比较候选任务和今日已完成任务的相似度，重复或相似的任务会过滤；与未完成任务相似时会倾向生成优化版。

### 55. 相似度如何判断？

当前主要使用关键词重叠和字符串相似度，后续可以扩展为 embedding 语义相似度。

### 56. 任务为什么要让用户选择？

AI 任务可能不完全符合用户当前意愿，让用户选择可以增强可控性，也避免任务列表被自动污染。

### 57. 如果用户今天已经完成“快走 20 分钟”，AI 还会生成类似任务吗？

原则上不会。系统会过滤与今日已完成任务相似的候选任务。

### 58. 如果用户有伤病史，任务生成会考虑吗？

会。任务生成上下文中包含伤病史，运动类建议会尽量避免不适合的高冲击动作。

## 七、答辩总结类高频问题

### 59. 你们项目最大的亮点是什么？

亮点是把 AI 嵌入完整健康管理流程：自然语言解析、知识增强建议、风险识别、候选任务生成和打卡反馈。

### 60. AI 在项目里不是简单聊天，体现在哪里？

AI 参与结构化解析、风险判断、知识增强建议和任务生成，并通过后端校验和用户确认控制结果落库。

### 61. 如何保证 AI 结果可靠？

通过数据库字段约束 Prompt、JSON 输出要求、后端校验、低置信度提示、高危拦截和用户确认保存机制共同保证。

### 62. 如果老师问“你们实现到什么程度”，怎么回答？

主流程已经打通，包括登录、档案、记录、AI 解析、建议、任务、趋势和个人中心，可以支撑完整演示。

### 63. 如果老师问“项目最核心的数据流是什么”，怎么回答？

用户输入健康记录，后端解析并保存为结构化数据，趋势服务聚合分析，AI 基于用户数据和知识库生成建议，再转化为候选任务。

### 64. 如果老师问“系统如何体现个性化”，怎么回答？

个性化体现在档案信息、健康目标、伤病/过敏史、历史健康记录、任务完成情况和知识检索上下文都会参与建议和任务生成。

### 65. 如果老师问“系统边界在哪里”，怎么回答？

系统只做日常健康管理辅助，不做医疗诊断；涉及病痛、高危症状或用药问题时会提示及时就医。

### 66. 一句话介绍项目？

HealthMate 是一个结合自然语言记录、知识增强 AI 建议和任务打卡反馈的日常健康管理系统。

